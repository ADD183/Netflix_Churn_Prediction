services:
  - type: web
    name: netflix-churn-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python training/train_model.py
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health